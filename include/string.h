#include "file.h"

void putc1( EMP_FILE, unsigned char );
const void gfprintf( EMP_FILE, const char*, ...);
const void gsprintf( INT8U*, const char*, ...);
const void gprintf( const char *, ...);
